import { ActiveRoom, Rooms, User, Winner } from './type';

export const db: User[] = [];

export const gamesDb: Rooms[] = [];

export const winnersDb: Winner[] = [];

export const activeRooms: ActiveRoom[] = [];
